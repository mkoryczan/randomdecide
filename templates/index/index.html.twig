{% extends "base.html.twig" %}

{% block content %}
    <div class="container">
        <div class="d-flex align-center justify-content-center text-center mt-5">
            <div class="row">
                <div class="col-12">
                    <h1>{{ 'homepage.title'|trans }}</h1>
                </div>
                <div class="col-12">
                    <p>{{ 'homepage.subtitle'|trans }}</p>
                    <a href="{{ path('draws_list') }}" class="btn btn-primary">{{ 'buttons.goto.drawsList'|trans }}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}